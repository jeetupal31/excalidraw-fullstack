# Server REST base URL (optional — defaults to the deployed Render URL)
VITE_API_BASE_URL="http://localhost:3000"

# Server WebSocket base URL (optional — derived from the API URL if unset)
VITE_WS_BASE_URL="ws://localhost:3000"
